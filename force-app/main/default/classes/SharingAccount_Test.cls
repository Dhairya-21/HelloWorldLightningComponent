@isTest
public class SharingAccount_Test {
    @isTest static void SharingAccount_Test() {
        List<Account> Acc = new List<Account>();
        Account a = new Account(Name = 'Dhairya', Rating = 'Hot');
        acc.add(a);
        Test.startTest();
        SharingAccount SA = new SharingAccount();
        SA.SharingAccount(Acc);
        Test.stopTest();
    }
}