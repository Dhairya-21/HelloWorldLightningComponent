trigger AccountT on Account (after insert, after update) {
    // AprrovalAccount a = new AprrovalAccount();
    // a.ApprovalAccount(Trigger.new);

    // CreateContactAccount c = new CreateContactAccount();
    // c.CreateContactAccount(Trigger.new);

    // DeleteSameRecords d = new DeleteSameRecords();
    // d.DeleteSameRecords(Trigger.new);
    
    // EmailAlert e = new EmailAlert();
    // e.EmailAlert(Trigger.new);
    
    // SharingAccount sharing = new SharingAccount();
    // sharing.SharingAccount(Trigger.new);
}